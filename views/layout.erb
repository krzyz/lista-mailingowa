<!DOCTYPE HTML>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <link rel="stylesheet" href="<%= url('/stylesheets/style.css') %>" />
    <title>Lista mailingowa NKF</title>
  </head>
  <body>
    <header>
      <h1>Lista mailingowa Naukowego Koła Fizyków</h1>
      <% if login? %>
        <nav>
          <ul>
            <li><a href="<%= url('/admin') %>">Ślij mejl!</a></li>
            <li><a href="<%= url('/admin/haslo') %>">Zmień hasło!!</a></li>
            <li><a href="<%= url('/admin/lista') %>">Lista zapisanych!!!</a></li>
          </ul>
          <span class="userinfo">Jesteś zalogowany jako admin. <a href="<%= url('/logout') %>">Wyloguj się.</a></span>
        </nav>
      <% end %>
    </header>
    <section>
      <% if flash[:error] %>
        <p><%= flash[:error] %></p>
      <% end %>
      <% if flash[:notice] %>
        <p><%= flash[:notice] %></p>
      <% end %>

      <%= yield %>    </section>
    <footer>
    </footer>
  </body>
</html>
